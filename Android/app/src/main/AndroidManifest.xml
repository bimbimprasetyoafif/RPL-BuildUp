<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.buildup">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CariActivity"></activity>
        <activity android:name=".EstimasiActivity" />
        <activity android:name=".FormActivity" />
        <activity android:name=".VendorActivity" />
        <activity android:name=".DeskripsiRumahActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".KeranjangActivity" />
        <activity android:name=".TokoActivity" />
        <activity android:name=".DeskripsiBarangActivity" />
        <activity android:name=".LupaPasswordActivity" /> <!-- <activity android:name=".Profil" /> -->
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".MasukActivity"
            android:label="@string/title_activity_masuk"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ActivitySplash" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>